// when A is pressed, the computer randomly chooses a no# between -20 & 20. if that no# is between 0 & 10 display that no#, otherwise display X (less lines to use)
input.onButtonPressed(Button.A, function () {
	
})
// when B is pressed, the computer randomly chooses a no# between -20 & 20. if that no# is between 0 & 10 display that no#, otherwise display X (more lines)
input.onButtonPressed(Button.B, function () {
	
})
basic.forever(function () {
	
})
basic.forever(function () {
	
})
